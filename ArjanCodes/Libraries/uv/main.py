# Example usage script

print("Hello from UV project!")
print("UV is fast and easy to use.")

# This would work if requests is installed:
# import requests
# response = requests.get("https://httpbin.org/json")
# print(response.json())
